# 安全策略

## 支持范围

当前维护版本为 `1.x`。安全问题请不要先公开披露，可通过仓库的私密安全报告功能联系维护者。

## 本机与账号影响

- 脚本只执行本地文件清理，不访问网络，不登录账号，也不读取浏览器、SSH 或 Git 凭据。
- 用户临时文件可能包含尚未落盘的应用恢复数据；运行前请保存工作并关闭应用。
- Windows Update 下载缓存是可重新下载的，但清理后下一次更新可能耗费更多时间和流量。
- 回收站清空后不能通过回收站恢复。
- 管理员模式会短暂停止 Windows Update 和 BITS 服务，脚本使用 `finally` 恢复原先运行的服务。

## 报告漏洞

报告中请包含受影响版本、复现步骤、潜在影响和建议修复方案。请勿附带真实令牌、密码、个人文件或其他敏感信息。

